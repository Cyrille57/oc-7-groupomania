{
  "name": "groupomania-front",
  "version": "1.0.0",
  "description": "Réseau social entreprise",
  "main": "index.js",
  "scripts": {
    "start": "npm run",
    "sass": "sass --watch ./public/sass/main.scss:./public/css/style.css",
    "prefix": "postcss ./public/css/style.css --use autoprefixer -d ./public/css/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cyrille MOREL",
  "license": "ISC",
  "browserslist": "last 4 versions",
  "dependencies": {
    "bootstrap": "^5.0.2"
  }
}
